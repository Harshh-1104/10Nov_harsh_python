# Write a step-by-step guide to deploying the “Doctor Finder” project to GitHub.


first select the folder that you want to deploy to github
then open the terminal in that folder
and run the following commands

```

git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin paste_ur_repo_link
git push -u origin main


```